Vendored library: [pdf.js](https://github.com/mozilla/pdf.js) (`pdfjs-dist`) v4.6.82, Mozilla, Apache License 2.0.

Files `pdf.min.mjs` and `pdf.worker.min.mjs` are the unmodified `legacy/build` output from the
`pdfjs-dist` npm package, vendored here so the app doesn't depend on an external CDN.
